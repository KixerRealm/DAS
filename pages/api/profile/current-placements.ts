import {NextApiRequest, NextApiResponse} from "next";


export default function handler(
    req: NextApiRequest,
    res: NextApiResponse
) {

    // This should serve to access previous
    //  game attempts for a specific user so we can display it on the FE - profile - history

}
