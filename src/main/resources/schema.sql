create table if not exists places(
  id                 varchar(2500),
  lat                  float,
  lng                  float,
  name               varchar(2500),
  photo_reference    varchar(2500),
  place_type               varchar(2500)
);